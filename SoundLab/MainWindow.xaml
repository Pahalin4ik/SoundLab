<Window x:Class="SoundLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SoundLab.ViewModel"
        xmlns:view="clr-namespace:SoundLab.View"
        xmlns:local="clr-namespace:SoundLab"
        mc:Ignorable="d"
        Title="SoundLab" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainVM/>
    </Window.DataContext>
    <Grid Background="{StaticResource Color3}">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5, 0">
            <view:Buttons DataContext="{Binding KeyBindingsVM}" Width="336"/>
        </StackPanel>
        <view:SoundTree  Margin="15,0,15,0" Grid.Row="1" DataContext="{Binding SoundTreeVM}"/>
    </Grid>
</Window>
